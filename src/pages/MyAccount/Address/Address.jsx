import React from "react";
import classes from "./Address.module.scss";

const Address = () => {
  return (
    <div className={classes?.address_component}>
      <div className={classes?.page_title}>Address</div>
    </div>
  );
};

export default Address;

import React from "react";
import classes from "./Orders.module.scss";

const Orders = () => {
  return (
    <div className={classes?.orders_component}>
      <div className={classes?.page_title}>Orders History</div>
    </div>
  );
};

export default Orders;

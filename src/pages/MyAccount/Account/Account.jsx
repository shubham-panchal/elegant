import React from "react";
import classes from "./Account.module.scss";

const Account = () => {
  return (
    <div className={classes?.account_component}>
      <div className={classes?.page_title}>Account Details</div>
    </div>
  );
};

export default Account;
